# ROGUE — Python Dependencies
# Install via: pip install -r requirements.txt
# Or use: bash scripts/setup_python.sh

# LLM / AI
requests>=2.31.0

# Screen capture
mss>=9.0.1
Pillow>=10.0.0

# PC control
pyautogui>=0.9.54

# Overlay UI
# Tkinter is bundled with Python — install python3-tk via apt if missing
# PyQt6>=6.5.0  # optional: heavier but more capable overlay

# Audio / WebSocket server
websockets>=12.0
numpy>=1.24.0

# TTS (Piper is a binary — see scripts/install_piper.sh)
# piper-tts is invoked via subprocess, not pip

# Utilities
python-dotenv>=1.0.0
